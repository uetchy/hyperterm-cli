#!/usr/bin/env node

const program = require('commander')

program
  .version('0.0.1')
  .command('install [name]', 'Install one or more plugins')
  .command('uninstall [name]', 'Uninstall one or more plugins')
  .command('list', 'List plugins')
  .parse(process.argv)
